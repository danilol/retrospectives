<ul class="breadcrumb">
  <li><%= link_to @current_user.name, users_path %> <span class="divider">/</span></li>
  <li><b>Teams</b></li>
</ul>
<div class="row-fluid">
  <div class="span6">
    <div class="page-header">
      <h3>Teams</h3>
    </div>
    <table class="table table-hover">
      <thead>
        <tr>
          <th>Name</th>
          <th>Leader</th>
          <th>Members</th>
        </tr>
      </thead>
      <tbody>
        <% @teams.each do |team| %>
          <tr>
            <td><%= link_to team.name, edit_team_path(team.id) %></td>
            <td><%= team_leader_name(team) %></td>
            <td><%= link_to "Members", show_members_path(team) %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
    <%= link_to "New Team", new_team_path, class: "btn btn-inverse btn-large" %>
    <br />

  </div><!-- div:span6 -->
  <div class="span5">
    <img src="/assets/milhouse.png"  title="<%= @current_user.name %>" alt="Ralph Steadman Artwork"/>
  </div><!-- div:span6 -->
</div><!-- div:row -->

